1.网络主体结构：
   （1）model_parts.py包括网络各个模块的具体描述。包括基础卷积模块，下采样模块，sub_pixel模块等。
   （2）Unet.py 包含model_parts.py，基于其进行网络主体结构的搭建主要包括编码模块以及解码模块。
2.训练入口代码：
    （1）ERA5.py 基于python中的Dataset模块实现数据读取器，数据处理模块需要根据具体数据来更改，大体思路为将
                 所有数据路径保存为列表，然后不断从列表中读出每个数据的路径，之后根据此路径滚你读取数据。
    （2）train.py 需要导入入Unet.py以及ERA5.py进行模型的训练，主要包括模型训练模块Train_Net以及模型测试模块Val_Net。

关于模型训练的过程再Unet.py中已经调通，可以直接python Unet.py执行代码中的toy数据进行模型训练测试。后续需要根据
实际训练数据读数据读取器进行修改进行实际数据训练。